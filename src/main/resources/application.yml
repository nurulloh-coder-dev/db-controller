spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/db_controller}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:root1234}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    time-zone: Asia/Tashkent

  mail:
    host: smtp.gmail.com
    port: 587
    username: hubProject1234@gmail.com
    password: exix cpos ndym fpkj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  spring:
    ai:
      openai:
        api-key: ${GROQ_API_KEY}
        base-url: https://api.groq.com/openai
      chat:
        model: llama-3.3-70b-versatile


  data:
    redis:
      host: ${REDISHOST}
      port: ${REDISPORT}
      password: ${REDISPASSWORD}
      username: ${REDISUSER}
server:
  port: ${PORT:8080}

app:
  docs-folder: "/app/app-knowledge/"
  base-folder: "/app/Agents/"
  logs-folder: "/app/logs/"
  user-register: "/app/user-registery"

app-security:
  token:
    access-exp: 6000
    refresh-exp: 86400
    secret: bu_secret_key_uzunligi_kamida_32_byte_boliswqewqewqewq